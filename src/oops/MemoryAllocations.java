package oops;

public class MemoryAllocations {

	public static void main(String[] args) {
		
		/* Stack (LIFO): 
		 * Stack will store the method invocation & the corresponding local variable or reference variable.
		 * It's temporary memory location which will be cleared once the execution of that method is completed.
		 * 
		 * Heap :
		 * Heap is permanent memory & scope will be there through out the program execution.
		 * It will store constructor call & the corresponding global variables.
		 * Memory  of heap will be cleared only when reference become null by the garbage collector Deamon thread by invoking finally method.
		 * 
		 * Class memory - All the static members will get memory allocated in class memory
		 * 
		 */

	}

}
