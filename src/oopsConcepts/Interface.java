package oopsConcepts;

public class Interface {
	
	/* 100% abstract class is know as an interface.
	 * Interface can accept  public static final variable, abstarct method and default method (Introduced from Java 8).
	 * Inteface shouldn't contain constructor
	 * We can't create an object for interface but we can create a reference for it.
	 * Intercae can have a default method with method defination.
	 * To derive an interface we should use the keyword called implements
	 * Multiple inheritance is possible in the form of an interface.
	 * Interface can accept only public modifier.
	 */

}
